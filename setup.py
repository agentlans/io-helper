from setuptools import setup

setup(name='io-helper',
      version='0.0.1',
      description='Reads and writes common formats like plain text, CSV, JSON, and SQLite 3 databases',
      author='Alan Tseng',
      packages=['io_helper'],
      license='GPL3',
      )
